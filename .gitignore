
# macOS Finder metadata
.DS_Store

# vim plugins (plug.vim is installed on first startup)
plug.vim
plugged/

# don't care too much about small changes to btop
files/.config/btop/


# pyrate scratch captures (raw API requests; contain session tokens)
bin/pyrate/temp/
